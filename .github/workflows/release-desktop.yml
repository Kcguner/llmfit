name: Release Desktop (Multi-OS)

on:
  push:
    tags:
      - "v*-mac"
      - "v*-desktop"

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build-desktop:
    strategy:
      matrix:
        include:
          - target: aarch64-apple-darwin
            os: macos-latest
          - target: x86_64-apple-darwin
            os: macos-latest
          - target: x86_64-pc-windows-msvc
            os: windows-latest
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-22.04

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install dependencies (Ubuntu only)
        if: matrix.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev build-essential curl wget file libssl-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Tauri CLI
        run: cargo install tauri-cli --version "^2"

      - name: Install Frontend Dependencies
        working-directory: llmfit-desktop/frontend
        run: npm ci

      - name: Build Tauri app bundle
        run: cargo tauri build --target ${{ matrix.target }}
        working-directory: llmfit-desktop

      - name: Package artifacts
        shell: bash
        run: |
          DEST_DIR="${GITHUB_WORKSPACE}/release-assets"
          mkdir -p "$DEST_DIR"
          
          BASE="${GITHUB_WORKSPACE}/llmfit-desktop/target/${{ matrix.target }}/release/bundle"
          
          if [[ "${{ matrix.os }}" == "macos-latest" ]]; then
            APP=$(find "$BASE" -name '*.app' -maxdepth 3 2>/dev/null | head -1)
            if [ -n "$APP" ]; then
              cd "$(dirname "$APP")"
              tar czf "${DEST_DIR}/llmfit-desktop-${GITHUB_REF_NAME}-${{ matrix.target }}.app.tar.gz" "$(basename "$APP")"
              cd "${GITHUB_WORKSPACE}"
            fi
            find "$BASE" -type f -name "*.dmg" -exec cp {} "$DEST_DIR/" \; || true
          elif [[ "${{ matrix.os }}" == "windows-latest" ]]; then
            find "$BASE" -type f -name "*.msi" -exec cp {} "$DEST_DIR/" \; || true
            find "$BASE" -type f -name "*.exe" -exec cp {} "$DEST_DIR/" \; || true
          else
            find "$BASE" -type f -name "*.deb" -exec cp {} "$DEST_DIR/" \; || true
            find "$BASE" -type f -name "*.AppImage" -exec cp {} "$DEST_DIR/" \; || true
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: desktop-assets-${{ matrix.target }}
          path: release-assets/*

  release:
    needs: build-desktop
    runs-on: ubuntu-latest

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@70fc10c6e5e1ce46ad2ea6f2b72d43f7d47b13c3 # v8.0.0
        with:
          path: artifacts
          pattern: desktop-assets-*
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b # v2.5.0
        with:
          generate_release_notes: true
          files: artifacts/*
